<Window x:Class="WpfPriceApp.Views.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="비밀번호 확인" Width="380" SizeToContent="Height"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource Brush.Background}" ShowInTaskbar="True">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="6"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="18"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText" Grid.Row="0" Text="비밀번호 확인" FontSize="16" FontWeight="Bold"
                   Foreground="{StaticResource Brush.TextPrimary}"/>
        <TextBlock x:Name="PromptText" Grid.Row="2" Text="비밀번호를 입력하세요." FontSize="12"
                   TextWrapping="Wrap" Foreground="{StaticResource Brush.TextSecondary}"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="4" Height="32" Padding="8,4" VerticalContentAlignment="Center"
                     FontSize="14"/>
        <TextBlock x:Name="ErrorText" Grid.Row="5" Text="비밀번호가 올바르지 않습니다." Margin="0,6,0,0"
                   Foreground="{StaticResource Brush.Danger}" FontSize="12" Visibility="Collapsed"/>

        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="취소" Width="80" Height="32" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="확인" Width="80" Height="32" Style="{StaticResource PrimaryButton}"
                    Click="Ok_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
