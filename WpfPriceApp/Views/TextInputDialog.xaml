<Window x:Class="WpfPriceApp.Views.TextInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="입력" Width="420" SizeToContent="Height" MinHeight="0"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource Brush.Background}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="6"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="14"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="18"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText" Grid.Row="0" FontSize="16" FontWeight="Bold"
                   Foreground="{StaticResource Brush.TextPrimary}"/>
        <TextBlock x:Name="PromptText" Grid.Row="2" Text="입력하세요" FontSize="12"
                   TextWrapping="Wrap" Foreground="{StaticResource Brush.TextSecondary}"/>
        <TextBox x:Name="InputBox" Grid.Row="4" Height="32" Padding="8,4" VerticalContentAlignment="Center"
                 FontSize="14"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="취소" Width="80" Height="32" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="확인" Width="80" Height="32" Style="{StaticResource PrimaryButton}"
                    Click="Ok_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
